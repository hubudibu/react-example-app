const initialState = [
  {
    text: "Sleep at least 7 hours",
    checked: false
  }
];

const todos = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default todos;
